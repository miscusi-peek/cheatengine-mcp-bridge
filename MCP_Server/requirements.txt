# Cheat Engine MCP Bridge - Python Dependencies
# Install: pip install -r requirements.txt

# MCP SDK (Model Context Protocol) - Required for AI agent communication
# Upper bound <2.0.0 guards against breaking API changes in a future major release.
mcp>=1.0.0,<2.0.0

# Windows API bindings - Required for Named Pipe communication
# Upper bound <400 guards against unexpected breaking changes in a future major release.
pywin32>=306,<400
